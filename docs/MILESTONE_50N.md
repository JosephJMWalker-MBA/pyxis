# Milestone 50N — prove second-basis bare overlay-v2 fresh-process product relaunch

Decision: **D268**  
Issue: **#266**

## Concrete researcher action

50M closes the second-basis 37B persistence gap for a changed-basis lineage that
contains one bare exact-range selection while its retained first-root ancestry also
contains a bare saved passage.

The next concrete action is:

> After restarting Pyxis, take that saved second-basis overlay-v2 and put the
> researcher back into the established second-basis-epoch product without manually
> re-entering the changed-basis locator set.

## Reuse review

The existing persisted second-epoch launch path already consumes one explicit 37B
path through established public boundaries:

```text
research-shell --second-basis-epoch-overlay <path>
→ load second-basis overlay
→ public 37A fresh reconstruction
→ persisted-path second-epoch lineage proof
→ established second-basis-epoch first-checkpoint product
```

The read-only inspection command uses the same loader, fresh reconstruction, and
lineage proof before projecting the established second-epoch authority report.

50M changed only the durable 37B codec/version dispatch. Above that boundary:

- the typed second-basis re-entry plan is unchanged;
- public 37A is unchanged;
- persisted-path lineage proof contains no overlay-v1 discriminator;
- the CLI already uses the public loader rather than inspecting document version;
- the second-basis product consumes typed lineage rather than raw overlay shape; and
- authority inspection consumes the same proven typed lineage.

## Decision

50N is **proof-only**.

No production-code changes.

The existing persisted 37B launch, second-basis product, CLI dispatch, lineage proof,
and read-only inspection seams are sufficient for the 50M overlay-v2.

## Executable product crossing

The focused proof creates a real 50M second-basis overlay-v2 through the public
persistence path.

That fixture is intentionally not simplified.

Its retained ancestry includes:

```text
first-root bare saved passage
→ first-root overlay-v2
→ ordinary continuation
→ cumulative first-root continuation
→ second bare saved passage
→ second changed basis
→ second root
→ explicit adoption
→ 46E / 37A proof
→ 46F / 37B overlay-v2
```

The written document is explicitly:

`pyxis.chromium.research_second_basis_epoch_reentry_locator_overlay.v2`

The exact persisted path is then supplied to the actual CLI entry point:

```text
pyxis research-shell --second-basis-epoch-overlay <overlay-v2>
```

## Existing launch path remains authoritative

The CLI performs the historical persisted second-epoch launch sequence unchanged:

1. public second-basis overlay decode;
2. public fresh 37A reconstruction;
3. public persisted-path second-epoch lineage proof;
4. established second-basis-epoch product dispatch.

The proof fails if the command falls back to:

- Workspace construction;
- ordinary research-session re-entry;
- root-backed mode;
- root-backed continuation mode; or
- controller-only mode.

None of those fallbacks is required.

## Freshness and launch-location context

The product receives one:

`ChromiumResearchSecondBasisEpochShellLineage`

whose `overlay_source` is exactly the caller-supplied overlay-v2 path resolved as
launch-location context.

The retained re-entry is a new fresh reconstruction.

It is not:

- the earlier 46E fresh result; or
- the fresh 37A object retained inside the earlier 37B checkpoint result.

The process boundary is therefore crossed through durable configuration and fresh
record relinking rather than old Python-object identity.

## Both bare passages survive relaunch

The fresh second-epoch lineage contains two distinct real loaded bare selections.

The retained first-root ancestry still contains one:

`ChromiumPageResearchLoadedParagraphTextSelectionRecord`

with selected text:

`"Bare"`

and no manufactured note.

The second changed-basis layer also contains a distinct real:

`ChromiumPageResearchLoadedParagraphTextSelectionRecord`

with selected text:

`"Bare"`

and no manufactured note.

The proof establishes object-level placement in the reconstructed lineage:

- the first bare member remains the exact loaded member retained by the first-root
  successor working set and prior-continuation endpoint working set;
- the second bare member remains the exact loaded member retained by the second-root
  successor working set and second-basis endpoint working set.

Thus the nested process-boundary reconstruction preserves note absence at both
changed-basis layers.

## Root and endpoint continuity

The fresh launch is tied back to the durable lineage by the established authority
checks.

The focused proof retains:

- the exact first-root identity through nested first-root ancestry;
- the exact second-root record SHA-256; and
- the exact second-basis terminal edge SHA-256.

Those digests remain record/content identity anchors only. They do not establish
authorship, trusted time, chronology, semantic support, or citation authority.

## Deterministic read-only inspection

50N also drives the actual CLI inspection command twice:

```text
pyxis research-inspect --second-basis-epoch-overlay <overlay-v2>
```

Both runs emit byte-identical deterministic JSON.

The output exactly equals serialization of the existing shared projection:

```text
explicit overlay-v2 path
→ public loader
→ fresh 37A reconstruction
→ persisted-path second-epoch lineage proof
→ inspect second-basis launch
→ existing deterministic serializer
```

The report remains:

`pyxis.chromium.research_second_basis_epoch_authority_inspection.v1`

with role:

`read_only_inspection_not_authority`

and launch family:

`persisted 37B second-basis-epoch launch`

The overlay-v2 path remains launch-location context only.

No inspection version or overlay-v2-specific inspection branch is added.

## No version branching above the loader

50N adds no overlay-v2-specific CLI flag, shell, lineage object, product mode, or
inspection format.

The durable version distinction remains owned by the public 37B loader.

Above that boundary, the system continues to operate on the established typed
second-basis plan, re-entry result, launch-lineage proof, product shell, and
authority-inspection structures.

## Repository Zero proof

Test-only head:

`0bdf39e0981bc07b7064979b9499cdca43d3ef27`

passed the complete Repository Zero suite on:

```text
Python 3.11
Python 3.12
Python 3.13
Python 3.14
```

with no production-code correction.

The documentation-complete exact head must pass the same matrix before merge.

## Scope

50N changes only:

- one focused CLI relaunch/inspection proof module;
- this milestone record; and
- compact README continuity.

It does not change:

- CLI implementation;
- second-basis overlay loading;
- public 37A reconstruction;
- persisted-path lineage proof;
- second-basis UI products;
- authority inspection;
- persistence formats;
- evidence formats; or
- browser behavior.

## Explicit stop boundary

50N proves only consumption of the 50M overlay-v2 through the already-established
persisted second-basis restart product.

Do not infer:

- 37C first continuation above v2-backed second-basis ancestry;
- 37D cumulative continuation above v2-backed second-basis ancestry;
- third-basis bare persistence;
- automatic overlay/path discovery;
- latest/current/head authority;
- launch-path mutation;
- browser reacquisition or mutation;
- search or indexing;
- tags;
- export;
- citation authority; or
- semantic interpretation.

Any continuation checkpoint above this v2-backed second-basis launch requires its own
concrete researcher-action review.

## Compact result

After 50N, the second-basis bare-passage path is mechanically proven across an actual
persisted relaunch boundary:

```text
first-root bare cumulative ancestry
→ second bare changed basis
→ 46E / 37A fresh proof
→ 37B overlay-v2 persistence
→ process restart
→ research-shell --second-basis-epoch-overlay
→ public v2 decode
→ fresh 37A reconstruction
→ persisted-path second-epoch lineage proof
→ established second-basis first-checkpoint product
```

and the same persisted overlay-v2 can be projected through the existing deterministic
read-only second-basis authority-inspection surface without adding another product or
format.
