# Milestone 50S — relaunch third-basis bare overlay-v2

Decision: **D273**  
Issue: **#276**

## Concrete researcher action

50R proves that one exact three-bare third-basis session can be persisted as strict
public-40B overlay-v2.

The next concrete researcher action is:

> Close the process, later supply that exact persisted overlay-v2 path, and resume the
> established third-basis product without losing any of the three bare saved passages
> or inventing a version-specific launch/inspection family.

## Reuse review

The persisted third-basis launch stack above public 40B was already format-agnostic.

Interactive launch:

```text
research-shell --third-basis-epoch-overlay <explicit path>
→ public 40B loader
→ public 40A fresh reconstruction
→ persisted-path 41A lineage proof
→ established inspectable third-basis first-checkpoint product
```

Read-only inspection:

```text
research-inspect --third-basis-epoch-overlay <explicit path>
→ public 40B loader
→ public 40A fresh reconstruction
→ persisted-path 41A lineage proof
→ established third-basis authority inspection-v1
→ deterministic JSON
```

50R localized v1/v2 dispatch inside the public 40B document loader.

No code above that loader asks which overlay version was supplied.

## Decision

50S is **proof-only**.

No production-code changes.

No CLI changes.

No product changes.

No shell-lineage changes.

No inspection-format changes.

## Full persisted ancestry

The focused proof creates the real 50R history:

```text
first-root bare saved passage
→ first-root overlay-v2
→ first-root ordinary continuation
→ first-root cumulative continuation
→ second-basis bare saved passage
→ second-basis 37B overlay-v2
→ second-epoch ordinary 37C-v1 continuation
→ second-epoch cumulative 37D / 37C-v1
→ third bare saved passage
→ 44A
→ 47A transition
→ 47B third root
→ 47C first edge
→ 47D adoption
→ 47E / public 40A fresh proof
→ 47F / public 40B overlay-v2
```

The saved third-basis document is then treated as the sole explicit persisted entry
configuration for the new process.

## Public loader and 41A proof

The v2 path is opened through the actual public 40B loader.

That loader returns the established typed public-40A plan.

Unchanged public 40A freshly reconstructs the complete three-root session.

Existing 41A then reopens the exact supplied 40B-v2 path and performs another fresh
reconstruction before binding it to:

`ChromiumResearchThirdBasisEpochShellLineage`

The lineage therefore retains a genuinely fresh result rather than either:

- the earlier 47E fresh proof; or
- the mandatory fresh result produced during 50R persistence.

The exact caller-supplied path is retained only as launch-location context.

## All three bare passages survive restart

The reconstructed lineage retains three distinct real:

`ChromiumPageResearchLoadedParagraphTextSelectionRecord`

objects.

Each has exact selected text:

`"Bare"`

and no manufactured `.note`.

The first bare passage survives in retained first-root ancestry.

The second survives in retained second-basis ancestry and through the cumulative
second-epoch continuation.

The third survives in the freshly reconstructed third-basis layer.

The third bare object is retained by identity in both:

- the fresh third-root successor working set; and
- the fresh final third-basis endpoint working set.

## Durable identity continuity

The relaunch preserves exact:

- first-root identity;
- second-root identity;
- third-root identity;
- third-basis declaration identity; and
- third-basis terminal edge identity.

These hashes remain record/content identity anchors only. They do not establish
authorship, authenticity, trusted time, chronology, semantic support, or citation
authority.

## Existing CLI product dispatch

The focused proof invokes the actual command:

```text
pyxis research-shell --third-basis-epoch-overlay <40B-v2>
```

The CLI uses the existing public 40B loader, public 40A reconstruction, and 41A
lineage proof.

It dispatches the established dedicated third-basis-epoch product family.

The proof rejects fallback to:

- Workspace;
- ordinary research-session;
- root-backed;
- root-backed-continuation;
- second-basis;
- second-basis-continuation;
- controller-only; or
- third-basis-continuation products.

No v2-specific CLI flag or receiver is introduced.

## Existing first-checkpoint product

The proven v2-backed 41A lineage instantiates the established inspectable
third-basis first-checkpoint product.

The product retains:

- the exact 41A lineage;
- the exact freshly reconstructed third-basis re-entry; and
- that re-entry's exact governed controller.

Its read-only launch provenance records the supplied 40B-v2 path only as location
context.

No v2-specific controls appear.

## Deterministic inspection

The same persisted v2 path is supplied twice to:

```text
pyxis research-inspect --third-basis-epoch-overlay <40B-v2>
```

Both runs emit byte-identical JSON.

The output exactly equals the established shared third-basis launch-inspection
serialization.

It remains:

`pyxis.chromium.research_third_basis_epoch_authority_inspection.v1`

with role:

`read_only_inspection_not_authority`

and launch family:

`persisted 40B third-basis-epoch launch`

The v2 overlay path is launch-location context only.

First-root, second-root, third-root, and launch-endpoint identity anchors remain the
existing fields.

## Fail-closed reconstruction

After a valid 40B-v2 document exists, the focused proof tampers with the explicitly
referenced third-root artifact.

The 40B-v2 document still strictly decodes as locator configuration.

Existing 41A then fails when it attempts fresh public-40A authority reconstruction.

Thus:

```text
valid persisted locator configuration
!=
freshly reconstructed research authority
```

remains enforced.

## Repository Zero proof

Test-only head:

`138dba2b6f4a81e24aade928227f1bf97049984a`

passed the complete Repository Zero suite on:

```text
Python 3.11
Python 3.12
Python 3.13
Python 3.14
```

with no production-code correction.

The documentation-complete exact head must pass the same matrix before merge.

## Scope

50S changes only:

- one focused persisted third-basis v2 relaunch proof module;
- this milestone record; and
- compact README continuity.

It does not change:

- public 40A;
- public 40B;
- 41A lineage;
- CLI behavior;
- third-basis product classes;
- authority inspection;
- 40C/40D continuation formats;
- first-/second-basis formats;
- browser behavior; or
- semantic interpretation.

## Explicit stop boundary

50S proves only persisted launch and inspection consumption of third-basis overlay-v2.

Do not infer:

- 47G in-process typed handoff from a v2 persistence result;
- 40C first persisted continuation above v2-backed third-basis ancestry;
- 40D cumulative continuation above that ancestry;
- fourth-basis bare persistence;
- arbitrary Nth-basis version propagation;
- automatic locator discovery;
- current/latest/head authority;
- browser mutation;
- search or indexing;
- tags;
- export;
- citation authority; or
- semantic interpretation.

The next researcher-action review should choose a concrete action rather than extend
the version pattern automatically.

## Compact result

After 50S:

```text
third-basis overlay-v2
→ explicit persisted path
→ public 40B version dispatch
→ unchanged public 40A reconstruction
→ unchanged 41A path proof
→ established third-basis first-checkpoint product
→ unchanged deterministic inspection-v1
```

All three nested bare saved passages survive the actual restart boundary.
