# Milestone 50X — persist and promote 40D after bare 47G → 40C → 41E

Decision: **D278**  
Issue: **#286**

## Concrete researcher action

50W proves that the uninterrupted bare third-basis workflow reaches the existing blank
40D cumulative form through:

```text
bare 40B-v2
→ pathless 47G
→ ordinary rollover
→ explicit 40C-v1
→ pathless 41E
→ cumulative receiver
→ ordinary rollover
→ blank 40D form
```

The next concrete action is:

> Supply the current persisted 40C location and the new cumulative destinations,
> complete that 40D checkpoint, and continue from the freshly promoted cumulative
> state without allowing the operation path to become launch authority.

## Reuse review

50U already proves unchanged public 40D can extend a 40C-v1 whose fixed direct
ancestry anchor is a third-basis 40B-v2.

50W already proves the exact 47G → 40C → 41E same-process composition reaches the
existing cumulative product with pathless launch provenance.

The only missing crossing was successful 40D persistence and promotion from that
pathless 41E receiver.

## Decision

50X is **proof-only**.

No production-code changes.

No persistence-format changes.

No handoff changes.

No CLI or inspection-format changes.

## Starting authority

The focused proof creates a real third-basis public-40B overlay-v2 retaining three
distinct bare saved passages.

Its exact public-40B fresh result enters the established pathless 47G first-checkpoint
receiver.

The researcher performs one ordinary rollover and explicitly supplies the current
40B-v2 path to unchanged public 40C.

Public 40C writes exact locator-only:

`pyxis.chromium.research_third_basis_epoch_continuation_locator_overlay.v1`

and freshly reconstructs the complete three-bare ancestry.

The exact 40C `checkpoint.fresh_reentry` then crosses existing explicit 41E into the
pathless cumulative receiver.

## Pathless 41E launch remains exact

The cumulative receiver begins with:

```text
third_basis_epoch_continuation_launch_lineage = None
third_basis_epoch_continuation_handoff_reentry = exact 40C handoff
third_basis_epoch_continuation_reentry = exact 40C handoff
research_controller = exact 40C handoff controller
```

Its immutable launch inspection remains:

```text
launch family: in-process 41E typed continuation handoff
launch location context: none
```

The existence of a persisted 40C checkpoint does not backfill that path into the
handoff receiver.

## One additional rollover

The researcher makes one more ordinary governed endpoint revision and rollover.

The live controller becomes the visible one-hop candidate.

The existing 40D cumulative form mounts with all four fields blank:

1. explicit current 40C/40D overlay path;
2. explicit chosen successor edge path;
3. cumulative declaration destination; and
4. next continuation-overlay destination.

The inspection current state deliberately remains on the last proven typed 40C
continuation at this stage.

Thus:

```text
visible one-hop candidate
!=
promoted typed cumulative continuation
```

remains intact.

## Explicit 40D operation paths

The researcher explicitly supplies:

- the current persisted 40C-v1 path;
- the chosen new successor edge;
- a no-overwrite cumulative declaration destination; and
- a no-overwrite next overlay destination.

Those are operation inputs only.

They do not become launch provenance.

## Direct 40B-v2 anchor remains fixed

Unchanged public 40D decodes and freshly reconstructs the supplied current 40C state.

The next plan preserves exactly:

`current_plan.prior_third_basis_epoch_overlay_source`

which remains the original third-basis 40B-v2 path.

It does **not** replace that anchor with the current 40C path.

The next declared-edge tuple is exactly:

```text
current declared edges
+ explicitly chosen successor
```

## Next overlay remains locator-only 40C-v1

The resulting durable document remains exactly:

`pyxis.chromium.research_third_basis_epoch_continuation_locator_overlay.v1`

with only:

```text
format
prior_third_basis_epoch_overlay_source
declared_edge_sources
declaration_source
```

It does not copy:

- bare exact-range-selection member locators;
- capture-source paths;
- selection-source paths;
- changed working-set/note locators;
- transition locators; or
- root locators.

The expanded third-basis member vocabulary remains owned only by the directly
referenced 40B-v2 boundary.

## Prior 40C remains immutable

The current 40C overlay bytes are recorded before cumulative persistence.

After successful 40D, those bytes remain exact.

40D writes only the new cumulative declaration and the new no-overwrite continuation
overlay.

## Three bare passages and three roots survive

The fresh 40D result retains all three real bare loaded selections with no manufactured
notes.

It also retains exact first-, second-, and third-root identities.

The cumulative terminal endpoint equals the explicitly chosen visible one-hop
successor by durable edge identity.

## Successful 40D is the promotion boundary

Only after public 40D succeeds does the cumulative receiver promote:

```text
third_basis_epoch_continuation_reentry = result.fresh_reentry
research_controller = result.fresh_reentry.controller
```

The visible one-hop candidate is removed as a provisional surface.

The retained rollover is cleared.

The cumulative form is removed.

Endpoint revision unlocks for the next researcher action.

## Inspection promotion remains narrow

Immutable 41E launch provenance remains the exact same object.

Its launch location remains `None` even though the researcher explicitly supplied a
current 40C path for the 40D operation.

Only current governed-state inspection advances, to:

```text
kind: typed third-basis-epoch continuation
source: 40D cumulative promotion after in-process 41E handoff
declared continuation edge count: 2
endpoint: exact fresh cumulative endpoint
```

Thus:

```text
explicit operation path
!=
handoff launch path
```

and:

```text
successful proof-gated 40D
=
typed cumulative promotion
```

## Repository Zero proof

Executable head:

`2bbd1a7a4193e8a828f83d22aed9e7706b7b638e`

passed the complete Repository Zero suite on:

```text
Python 3.11
Python 3.12
Python 3.13
Python 3.14
```

with no production-code correction.

The documentation-complete exact head must pass the same matrix before merge.

## Scope

50X changes only:

- one focused same-process cumulative-promotion proof;
- this milestone record; and
- compact README continuity.

It does not change:

- public 40A;
- public 40B v1/v2;
- public 40C;
- public 40D;
- 47G;
- 41E;
- CLI behavior;
- authority-inspection formats;
- first-/second-basis formats;
- browser behavior; or
- semantic interpretation.

## Explicit stop boundary

50X proves only successful 40D persistence/promotion after the exact same-process:

```text
bare 40B-v2
→ pathless 47G
→ 40C-v1
→ pathless 41E
→ 40D cumulative promotion
```

Do not infer:

- automatic cumulative persistence;
- implicit current-overlay authority;
- path backfill into handoff provenance;
- fourth-basis behavior;
- arbitrary-depth handoff/version propagation;
- automatic path discovery;
- current/latest/head authority;
- browser mutation;
- search or indexing;
- tags;
- export;
- citation authority; or
- semantic interpretation.

The concrete three-basis bare workflow is now closed across both persisted restart and
uninterrupted same-process continuation through cumulative promotion.

The next milestone should begin from an actually missing researcher job rather than
lineage symmetry.

## Compact result

After 50X:

```text
three-bare third-basis 40B-v2
→ pathless 47G
→ explicit 40C-v1
→ pathless 41E
→ ordinary rollover
→ explicit unchanged 40D
→ same fixed 40B-v2 ancestry
→ next unchanged 40C-v1
→ fresh cumulative typed promotion
→ pathless immutable 41E launch provenance retained
```
