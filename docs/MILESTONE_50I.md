# Milestone 50I — prove bare-selection overlay-v2 fresh-process product relaunch

Decision: **D263**  
Issue: **#256**

## Concrete researcher action

50H closes the first-root 35C persistence gap for a changed-basis lineage that
contains one bare exact-range selection.

The next concrete action is:

> After restarting Pyxis, take that saved overlay-v2 and put the researcher back
> into the established root-backed product without requiring manual re-entry of the
> changed-basis locator set.

## Prior-art and reuse review

The existing persisted-launch product path already consumes one explicit 35C path
through public application boundaries:

```text
research-shell --root-backed-overlay <path>
→ load_chromium_research_root_backed_session_reentry_plan_document(...)
→ reenter_chromium_research_root_backed_session(...)
→ prove_chromium_research_root_backed_session_shell_lineage(...)
→ existing root-backed product shell
```

The read-only inspection path uses the same public loader, fresh re-entry, and
lineage proof before projecting the established authority-inspection report.

45A lineage proof compares:

- governed presentation;
- terminal durable edge identity; and
- retained 34A root identity.

It contains no overlay-v1 discriminator and no note-bearing member assumption.

50H already generalized the public first-root overlay loader so overlay-v1 and
overlay-v2 both produce the same established typed
`ChromiumResearchRootBackedSessionReentryPlan`.

Therefore the strongest initial hypothesis was that 50I required executable proof,
not another launch implementation.

## Decision

50I is **proof-only**.

No production code changes.

The existing first-root persisted launch and inspection seams are sufficient for
the 50H overlay-v2.

## Executable product crossing

The focused test creates a real bare-selection changed-basis lineage and persists it
through public 50H/35C.

The resulting configuration is explicitly:

`pyxis.chromium.research_root_backed_session_reentry_locator_overlay.v2`

That exact path is then supplied to the actual CLI entry point:

```text
pyxis research-shell --root-backed-overlay <overlay-v2>
```

The test does not invoke the lower-level functions in place of the CLI.

## Established launch path remains authoritative

The CLI still performs the historical one-root launch sequence unchanged:

1. public root-backed overlay decode;
2. public fresh 35B reconstruction;
3. public 45A persisted-path lineage proof;
4. existing root-backed product dispatch.

The executable proof fails if the command attempts to:

- build Workspace state;
- use the ordinary research-session re-entry shell; or
- fall back to a controller-only shell that would discard typed root-backed
  lineage.

None of those fallbacks is needed.

## Freshness and lineage identity

The root-backed product receives a
`ChromiumResearchRootBackedSessionShellLineage`.

Its `overlay_source` is exactly the caller-supplied overlay-v2 path resolved as
launch-location context.

Its retained re-entry is freshly reconstructed.

It is not:

- the 44F fresh re-entry object retained before 35C persistence; or
- the fresh 35B object retained inside the earlier 35C checkpoint result.

The new launch proves equivalent governed state through durable identities rather
than carrying an old Python object across the process boundary.

## Bare saved passage survives relaunch

The freshly relaunched changed working set still contains a real:

`ChromiumPageResearchLoadedParagraphTextSelectionRecord`

The focused proof establishes that:

- its exact selected text is still `"Bare"`;
- no note object is manufactured;
- the fresh 34A root successor working set retains that same freshly loaded bare
  member; and
- the fresh declared endpoint working set retains that same member.

Therefore:

```text
fresh process
+ persisted overlay-v2
!=
note synthesis
```

and:

```text
bare saved passage semantics
survive
35C persistence → process boundary → 35B relink → 45A launch proof
```

## Root and endpoint continuity

The fresh launch is mechanically tied back to the durable changed-basis lineage.

The executable proof checks that the fresh re-entry retains:

- the exact 34A root record SHA-256 established before persistence; and
- the exact declared endpoint edge record SHA-256 established before persistence.

Those digests remain record/content identity anchors only. 50I adds no authorship,
trusted-time, chronology, semantic-support, or citation claim.

## Read-only inspection inheritance

50I also drives the actual CLI inspection command twice:

```text
pyxis research-inspect --root-backed-overlay <overlay-v2>
```

Both runs emit byte-identical deterministic JSON.

The CLI output exactly equals serialization of the existing shared projection:

```text
explicit overlay-v2 path
→ public loader
→ public 35B fresh re-entry
→ public 45A lineage proof
→ inspect_chromium_research_root_backed_session_launch(...)
→ existing deterministic serializer
```

The report remains:

`pyxis.chromium.research_root_backed_session_authority_inspection.v1`

with role:

`read_only_inspection_not_authority`

and launch family:

`persisted 35C root-backed launch`

The overlay-v2 path is presented only as launch-location context.

No new inspection format is necessary.

## No version branching above the loader

50I adds no CLI test for an artificial overlay-v2-specific command and no product
branching by format.

The durable version distinction remains owned by the public first-root 35C loader.

Above that boundary, the system continues to operate on the established typed plan,
re-entry result, lineage proof, and authority-inspection structures.

This is the intended reuse result of 50H.

## Repository Zero proof

The test-only implementation head passed the complete Repository Zero suite on:

```text
Python 3.11
Python 3.12
Python 3.13
Python 3.14
```

with no production-code correction.

The documentation-complete exact head must pass the same matrix before merge.

## Scope

50I changes only:

- one focused CLI proof module;
- this milestone record; and
- compact README continuity.

It does not change:

- CLI implementation;
- root-backed overlay loading;
- 35B reconstruction;
- 45A lineage proof;
- root-backed UI shells;
- authority inspection;
- persistence formats;
- evidence formats; or
- second-/third-epoch products.

## Explicit stop boundary

50I proves only consumption of the first-root 50H overlay-v2 through the already
established restart product.

Do not infer:

- second-basis-epoch 37B bare-selection serialization;
- third-basis-epoch 40B bare-selection serialization;
- 31B plan-v2;
- automatic overlay/path discovery;
- latest/current/head authority;
- automatic adoption;
- browser highlighting or mutation;
- search or indexing;
- tags;
- export;
- citation authority; or
- semantic interpretation.

Any later-epoch persistence crossing still requires its own concrete
researcher-action review.

## Compact result

After 50I, the first-root bare-passage path is mechanically proven end-to-end across
an actual process-relaunch boundary:

```text
saved exact passage without note
→ changed-basis v2 working set
→ transition-v1
→ root-v1
→ edge-v1
→ explicit governed-session adoption
→ typed fresh 35B verification
→ proof-gated 35C overlay-v2 persistence
→ process restart
→ research-shell --root-backed-overlay
→ public v2 decode
→ fresh 35B reconstruction
→ 45A persisted-path lineage proof
→ established root-backed product
```

and the same persisted path can be projected through the existing deterministic
read-only authority-inspection surface without adding another product or format.
