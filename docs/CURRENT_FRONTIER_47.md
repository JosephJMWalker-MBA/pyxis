# Pyxis Current Frontier — Milestones 47A through 47G

> **Continuity status — implementation through Milestone 47G / D241.**  
> **Updated:** 2026-09-04  
> This compact record does not replace milestone documents, implementation, tests, executed CI, or earlier frontier files.

## Read this after `docs/CURRENT_FRONTIER_46.md`

Milestone 46 closed the second changed-basis product loop by explicitly handing the exact public-37B fresh proof into the established second-basis-epoch product.

The 47-series asked the next concrete product question:

> Can one exact current second-basis-epoch continuation explicitly cross a third changed evidence basis, establish and adopt the third-root-backed governed session, prove and persist three-root restartability, and then explicitly transfer the exact public-40B fresh proof into the already-established third-basis-epoch product?

Milestones 47A–47G now answer that question end to end.

## Completed third changed-basis product flow

```text
exact current 37C/37D second-basis-epoch continuation
→ inherited 44A changed-basis preparation
→ 47A persist third 33B transition
→ 47B persist third 34A revision root
→ 47C persist first 34B edge after third root
→ 47D explicitly adopt third changed-basis governed session
→ 47E freshly prove exact three-root reconstructability through public 40A
→ 47F persist verified third-basis restart overlay through public 40B
→ 47G explicitly hand exact public-40B fresh proof into established third-basis-epoch product
```

Every arrow is still a separate authority action.

```text
preparation
!= transition
!= root
!= adoption
!= fresh reconstruction
!= durable restart configuration
!= in-process product handoff
```

The 47-series therefore completes a third independently demonstrated changed-basis product crossing without claiming arbitrary-depth evidence-basis ancestry.

---

## 47A / D235 — explicit third changed-basis transition

47A reuses generic 44A changed-basis preparation but requires an exact current `ChromiumResearchSecondBasisEpochContinuationReentryResult` before exposing another public 33B crossing.

The current continuation must own the exact supplied controller object. Structural or presentation equivalence is insufficient for this in-process authority action.

All durable transition locators remain caller supplied:

- current prior endpoint edge;
- current prepared working set;
- current prepared working-set note;
- no-overwrite transition destination.

No path is inferred from preparation receipts, launch provenance, checkpoint paths, filenames, directories, or hashes.

Successful transition persistence leaves the mounted second-epoch continuation unchanged.

---

## 47B / D236 — explicit third 34A revision root

47B consumes one exact successful 47A transition and one genuinely new human root rationale.

The researcher explicitly supplies the current prior edge, changed working set, changed working-set note, third transition, and new no-overwrite root destination.

The transition is freshly relinked and the third root is freshly reopened through public 34A machinery before success.

The third root remains historical lineage. It does not itself adopt a third-basis-epoch controller.

---

## 47C / D237 — first ordinary edge after the third root

47C exposes the established public 34B bridge from the exact third root into ordinary post-root edge lineage.

Inputs remain deliberately narrow:

- new human rationale;
- explicit current third-root source;
- explicit no-overwrite first-edge destination.

The persisted edge is freshly reopened against the exact retained third root.

47C does not declare or adopt the third-basis session.

---

## 47D / D238 — explicit third changed-basis governed-session adoption

47D is the deliberate branch-changing action.

From the exact 47C root-plus-first-edge lineage, Pyxis reuses established root-started declaration/controller machinery and explicitly adopts the resulting third-root-backed governed session inside the shell.

The adoption is local and user authorized.

```text
successful 47D adoption
!= global latest/current/head
!= fresh-process restartability
```

No 40A reconstruction or 40B restart overlay is implied merely because the session is now mounted.

---

## 47E / D239 — fresh three-root reconstruction proof

47E proves that the exact historical 47D adoption can be freshly reconstructed through existing public 40A.

Every durable location required for the proof is explicitly supplied for the current operation:

1. current prior 37C/37D second-epoch continuation overlay;
2. current locator set for each appended third-basis evidence member;
3. changed working-set source;
4. changed working-set-note source;
5. third 33B transition source;
6. third 34A root source;
7. first post-third-root edge source;
8. third-root-backed declaration source.

The proof freshly reconstructs and compares all three retained root layers:

```text
retained first root
+ retained second root
+ third root
+ selected prior second-epoch continuation state
+ final third-basis governed state
```

The fresh 40A controller is proof evidence only. The mounted 47D controller remains active.

For raw 38F source launches, an explicitly supplied persisted 37C/37D overlay may be used for the proof, but that path is never backfilled into immutable launch provenance.

PR #200 merged 47E as `dbd45b5f5afd1b3d1cc76c86fcfd2d1ad13e04b1` after Repository Zero passed the complete suite on Python 3.11, 3.12, 3.13, and 3.14.

---

## 47F / D240 — persist verified third-basis restart overlay

47F consumes one exact successful 47E proof and delegates persistence to existing public 40B.

The researcher must explicitly re-supply:

1. the current prior 37C/37D second-epoch continuation-overlay source;
2. one new no-overwrite 40B third-basis overlay destination.

Public 40B remains authoritative for:

```text
candidate locator plan
→ mandatory fresh 40A three-layer reconstruction
→ retained first/second/third-root comparison
→ prior second-epoch continuation comparison
→ final third-basis governed-state comparison
→ strict no-overwrite overlay write
→ strict round-trip decode
```

47F persists the exact historical 47E target even if the mounted changed-basis product later rolls forward.

Persistence does not replace mounted governed state and does not mutate launch provenance.

For raw 38F source launches, explicit use of a persisted 37C/37D overlay for proof still does not create launch-path provenance.

PR #202 passed Repository Zero on Python 3.11–3.14 and merged as `7d79ba9d85c5beab7419533d68906f4977929efb`.

---

## 47G / D241 — explicit typed handoff into established third-epoch product

47G closes the remaining third changed-basis product seam.

Successful 47F persistence contains two distinct fresh 40A objects:

```text
47E verification_result.fresh_reentry
→ earned input retained by public 40B

40B mandatory independent fresh reconstruction
→ checkpoint.fresh_reentry
```

The exact 47G handoff subject is the latter:

```python
last_third_changed_basis_epoch_reentry_overlay
    .checkpoint
    .fresh_reentry
```

It is not:

- the saved 40B overlay path;
- the earlier 47E fresh object merely because durable state is equivalent;
- a new reconstruction loaded from disk;
- whichever controller happens to be mounted;
- a fabricated `ChromiumResearchThirdBasisEpochShellLineage`.

### Persistence and handoff remain separate

Successful 47F persistence alone leaves the changed-basis product mounted.

Only an explicit 47G action exits with the exact `checkpoint.fresh_reentry` object.

Normal close returns no handoff.

The saved 40B overlay may become unavailable after successful 47F persistence and before the 47G button press. The already-earned typed result still remains valid in-process authority because 47G performs no disk reload.

### Pathless initial third-epoch receiver

47G adds a concrete receiver that reuses the established third-basis-epoch first-checkpoint product without inventing persisted launch provenance:

```text
third_basis_epoch_launch_lineage = None
third_basis_epoch_handoff_reentry = exact 47G handoff
third_basis_epoch_reentry = exact 47G handoff
```

The persisted launch shell now also separates:

```text
third_basis_epoch_launch_lineage
!=
third_basis_epoch_reentry
```

while both still identify the same freshly proven state for ordinary persisted 40B launches.

After one explicit rollover, the pathless receiver exposes the existing 40C checkpoint form with every durable location blank.

### Read-only launch inspection

47G adds a concrete initial handoff inspection family:

```text
launch family: in-process 47G typed third-basis-epoch handoff
launch location context: none
first-root SHA-256
second-root SHA-256
third-root SHA-256
launch endpoint SHA-256
```

A later rollover preserves the exact immutable launch-provenance object and advances only current governed state.

### Executed evidence

PR #204 exact head `d8f06c73cd762b318c148d9884e98a82bea10bba` passed Repository Zero on Python 3.11, 3.12, 3.13, and 3.14 on the first submitted head.

It merged as `f39d8085161b70fcd0d6e1c87d30f4730d4e38b1`.

No corrective commit was required.

---

## Why there is no automatic 47H

The post-47G frontier review found no demonstrated missing lifecycle step inside the third changed-basis flow.

The established receiver already owns:

```text
47G exact initial third-epoch handoff
→ ordinary explicit rollover
→ public 40C first continuation checkpoint
→ explicit 41E typed handoff
→ repeatable public 40D cumulative continuation checkpointing
```

Persisted and in-process third-epoch launch families already have read-only authority inspection.

Adding another 47-series runtime action merely because 44 ended at H or because another letter is available would duplicate established third-epoch continuation machinery or invent authority without a researcher-facing requirement.

The 47-series should therefore be treated as complete through 47G unless a concrete missing product seam is demonstrated.

---

## Three independently proven changed-basis product crossings

The repository now contains three complete concrete product crossings:

### First crossing

```text
44A preparation
→ 44B transition
→ 44C root
→ 44D edge
→ 44E adoption
→ 44F fresh proof
→ 44G restart persistence
→ 44H typed handoff
```

### Second crossing

```text
inherited 44A preparation
→ 46A transition
→ 46B root
→ 46C edge
→ 46D adoption
→ 46E fresh proof
→ 46F restart persistence
→ 46G typed handoff
```

### Third crossing

```text
inherited 44A preparation
→ 47A transition
→ 47B root
→ 47C edge
→ 47D adoption
→ 47E fresh proof
→ 47F restart persistence
→ 47G typed handoff
```

This repetition is now architectural evidence.

It does **not** prove that ancestry semantics are generic.

It does show that several surrounding product procedures have now been independently demonstrated three times and may qualify for the same bounded-reuse review that Milestone 43 applied to cumulative continuation mechanics.

---

## Post-47G reuse review

The strongest candidate for the next development cycle is **bounded extraction of triply-proven changed-basis product mechanics**, not a fourth evidence-basis epoch.

Potentially reusable mechanics now independently demonstrated include:

- post-persistence exact typed handoff gating;
- separation of successful persistence from explicit mode promotion;
- retaining `checkpoint.fresh_reentry` as the in-process handoff subject;
- no disk reload during typed handoff;
- normal-close-versus-explicit-handoff runner behavior;
- blank explicit-locator form discipline;
- historical verification/persistence target retention;
- mounted-state preservation around non-promoting proof/persistence actions;
- Textual MRO event ownership that avoids double dispatch.

The surrounding authority semantics remain concrete:

- first crossing owns ordinary-session → one-root ancestry;
- second crossing owns one-root-continuation → second-root ancestry;
- third crossing owns second-epoch-continuation → third-root ancestry;
- reconstruction plans and restart overlays remain distinct 35B/35C, 37A/37B, and 40A/40B types;
- launch-lineage and inspection types remain concrete;
- root counts and ancestry proof obligations remain concrete.

Any extraction should therefore follow the 43-series rule:

> share only mechanics independently demonstrated reusable; leave ancestry interpretation and public authority surfaces concrete.

---

## Why a fourth evidence-basis epoch is not yet justified

The repository has lower-level evidence that another crossing could be represented by repeating established 33A/33B/34A/34B patterns and extending ancestry again.

That is not sufficient product justification.

A fourth concrete epoch would add another ancestry type, fresh-reentry type, persistence document, launch-lineage proof, inspection model, continuation family, and product lifecycle.

Before paying that cost, a researcher-facing requirement must demonstrate why the existing concrete third-epoch product plus repeatable 40C/40D continuation is insufficient.

Likewise, three completed crossings do not by themselves prove a safe generic:

```text
epoch[n]
ancestor_roots[]
recursive_parent_overlay
generic ancestry walker
```

The same restraint that prevented premature genericization before Milestone 43 still applies.

---

## Strongest compact statement through 47G

> From one exact current second-basis-epoch continuation and exact candidate evidence, Pyxis can explicitly prepare and persist a third changed evidence basis, establish and adopt its third-root-backed governed session, freshly prove and persist three-root restartability, and—only on a separate explicit human action—transfer the exact mandatory public-40B fresh proof into the established third-basis-epoch product without reloading or promoting the saved overlay path.

That third-basis product then already supports explicit 40C/41E/40D continuation.

---

## Authority boundaries still intentionally absent

Through 47G, Pyxis still does not infer or claim:

- a global current/latest/head research branch;
- chronology from paths, filenames, hashes, or filesystem timestamps;
- path equality as durable identity;
- persistent path authority from a prior successful operation;
- persistent path provenance for an in-process typed handoff;
- source authenticity, authorship, or trusted time from SHA-256 integrity;
- semantic improvement merely because an evidence basis changed;
- evidentiary support or citation authority merely because notes or selections exist;
- autonomous research or browser interaction authority;
- arbitrary-depth evidence-basis recursion;
- generic `epoch[n]` transition/adoption/restart machinery;
- a fourth evidence-basis epoch merely by analogy;
- automatic restart or mode promotion from successful persistence.

---

## Next decision frontier

Do not infer the next runtime milestone from numbering.

The next high-leverage review should ask:

> Which procedures have now been independently demonstrated across all three complete changed-basis product crossings and can be privately shared without weakening the concrete authority semantics of 44, 46, and 47?

This is a **reuse/refactor question**, not a new authority question.

A candidate extraction must meet the same standard established by 43A–43E:

```text
triply-proven repeated procedure
+ concrete authority checks remain outside helper
+ no public format/type widening
+ parity tests across all three real families
→ private bounded reuse
```

If no such extraction materially improves maintainability without obscuring authority, leave the duplication in place.

Only after that review should Pyxis consider another researcher-facing capability.

## Continuity rule

For future development sessions:

```text
implementation + executed tests + milestone record
> compact frontier continuity summary
> README/current-status wording
```

Read the compact chain in order:

1. `docs/CURRENT_FRONTIER.md`
2. `docs/CURRENT_FRONTIER_35_36.md`
3. `docs/CURRENT_FRONTIER_37_38.md`
4. `docs/CURRENT_FRONTIER_39_40.md`
5. `docs/CURRENT_FRONTIER_41_43.md`
6. `docs/CURRENT_FRONTIER_44.md`
7. `docs/CURRENT_FRONTIER_46.md`
8. this file, `docs/CURRENT_FRONTIER_47.md`

If a compact summary conflicts with implementation, tests, executed CI, or milestone-specific records, inspect the stronger evidence before changing behavior.
