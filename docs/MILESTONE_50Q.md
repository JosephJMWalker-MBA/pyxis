# Milestone 50Q — prove bare selection through third-basis 47E / 40A

Decision: **D271**  
Issue: **#272**

## Concrete researcher action

50P proves a cumulative second-basis continuation whose retained ancestry already
contains two bare saved passages:

- one in the first-root changed-basis lineage; and
- one in the second changed-basis lineage.

The next concrete researcher action is:

> Add a third bare saved source passage as the third changed-basis candidate, carry
> it through the established third-basis product flow, and prove the exact adopted
> third-basis session can be freshly reconstructed through public 40A without
> manufacturing a note.

## Reuse review

The application-layer third-basis reconstruction was already structurally ready.

Public 40A uses:

`ChromiumResearchWorkingSetMemberReentryLocator`

for its appended third-basis members.

That generalized union already includes:

`ChromiumResearchExactRangeSelectionReentryLocator`

and public 40A loads every appended member through the generalized member loader.

The generic working-set/change-preparation layer also already accepts a real loaded:

`ChromiumPageResearchLoadedParagraphTextSelectionRecord`

Therefore no 40A plan, loader, transition, root, edge, or adoption implementation
needed widening.

The actual gap was product-local to 47E:

- its Textual classifier knew only the three historical note-bearing member families;
- its summary unconditionally read `item.note.note_text`;
- its form always mounted a note-sidecar field; and
- its collector always required a note locator for non-comparison members.

## Decision

Widen only the 47E product-verification surface for one bare exact-range selection.

Production changes are limited to:

- `chromium_research_third_changed_basis_epoch_reentry_textual.py`; and
- `third_changed_basis_epoch_reentry_research_session_shell.py`.

Public 40A remains unchanged.

47A–47D remain unchanged.

No third-basis overlay-v2 is introduced.

## Full retained ancestry

The focused proof deliberately starts from the complete 50P lineage:

```text
first-root bare saved passage
→ first-root overlay-v2
→ ordinary first-root continuation
→ cumulative first-root continuation
→ second bare saved passage
→ second changed basis
→ second-root 37B overlay-v2
→ ordinary 37C-v1 continuation
→ cumulative 37D / next 37C-v1
```

That exact persisted cumulative second-epoch continuation is freshly lineage-proven
before the third changed-basis action begins.

The third candidate is a separate real bare exact-range-selection record.

## Existing 44A and 47A–47D carry the bare candidate unchanged

The third bare object passes through existing generic 44A preparation.

The focused proof establishes:

- `prepared.appended_items == (bare,)`;
- the exact original bare object is retained by identity;
- the changed working set uses working-set-v2;
- the changed working-set note uses note-v2; and
- the bare object is the final member of that prepared working set.

No note is attached to the bare member.

The same prepared state then crosses unchanged:

```text
47A third 33B transition-v1
→ 47B third 34A root-v1
→ 47C first post-third-root edge-v1
→ 47D explicit governed-session adoption
```

No production change is required at any of those boundaries.

## Bare member presentation

47E now classifies a loaded bare selection as:

`exact-range selection`

Its visible member summary states:

`Human note: none attached — saved source passage only`

It does not attempt to access a nonexistent `.note` attribute and does not render
`None` as authored text.

Historical paragraph-note, exact-range-note, and comparison-note presentation remains
unchanged.

## Bare locator collection

For one bare third-basis member, the 47E form mounts exactly:

- explicit current capture path; and
- explicit current selection-sidecar path.

It does **not** mount or require a note-sidecar input.

Those two paths construct the already-established:

`ChromiumResearchExactRangeSelectionReentryLocator`

No new locator type is introduced.

## Direct public-40A proof

Before relying on the Textual submit path, the focused proof directly calls the
existing 47E application verification adapter with the exact bare locator.

Public 40A successfully reconstructs the complete nested three-root lineage.

The newly loaded third-basis member is a real:

`ChromiumPageResearchLoadedParagraphTextSelectionRecord`

with exact selected text:

`"Bare"`

and no manufactured `.note` attribute.

That same loaded object is retained by identity in:

- the freshly reconstructed third-root successor working set; and
- the freshly reconstructed final third-basis endpoint working set.

The direct proof therefore isolates application correctness from UI event handling.

## All three bare passages survive fresh reconstruction

The public-40A result first reconstructs the explicit prior cumulative second-epoch
continuation.

That fresh prior ancestry still contains:

1. the first-root bare saved passage;
2. the second-basis bare saved passage; and
3. the newly appended third-basis bare saved passage above them.

Each remains a real loaded exact-range-selection record with no manufactured note.

The third-basis proof also preserves exact durable identity for:

- retained first root;
- retained second root;
- new third root;
- third-root-backed declaration; and
- final third-basis endpoint edge.

## Real 47E Textual proof

The real 47E form mounts only after exact successful 47D adoption.

All locator fields begin blank.

For the third-basis bare member, the focused proof confirms:

- one capture-source input exists;
- one selection-source input exists; and
- no note-source input exists.

The proof intentionally submits all other required paths while leaving
`selection_source` blank.

47E rejects before public 40A verification with:

`appended bare selection member 0 requires capture and selection paths`

The form remains unlocked.

After the explicit selection-sidecar path is supplied, the same real 47E product
successfully delegates to public 40A.

As with the earlier 50L Textual proof, the retry waits for Textual's transient button
`-active` state to clear before the second activation and then waits on the actual
verification state rather than interpreter scheduling timing.

## Mounted and inspection authority remain unchanged

Successful 47E proof is evidence of fresh reconstructability only.

It does not replace:

- the mounted adopted third-basis controller;
- the mounted research session;
- generic research re-entry state;
- the retained second-basis cumulative continuation; or
- immutable persisted second-epoch launch provenance.

Inspectable current-state projection also remains unchanged by the proof.

## 47F / public 40B remains deliberately closed

50Q does not widen third-basis durable restart persistence.

The existing 40B document codec remains:

`pyxis.chromium.research_third_basis_epoch_reentry_locator_overlay.v1`

and delegates member encoding/decoding to the frozen historical three-kind codec.

After successful bare 47E verification, the focused proof supplies that exact result
to existing 47F / public 40B persistence.

The strict overlay-v1 writer rejects the bare locator as an unsupported member
locator.

The destination is asserted not to exist after rejection.

Therefore:

```text
47E / 40A bare fresh reconstruction
does not imply
47F / 40B bare durable serialization
```

No third-basis overlay-v2 is introduced by 50Q.

## Repository Zero proof

Executable head:

`e90379ce0c0d2569b92fdcdfde321a6fdb10a5c4`

passed the complete Repository Zero suite on:

```text
Python 3.11
Python 3.12
Python 3.13
Python 3.14
```

The documentation-complete exact head must pass the same matrix before merge.

## Scope

50Q changes only:

- the existing 47E Textual member presentation;
- the existing 47E locator collector;
- one focused nested third-basis bare-selection proof module;
- this milestone record; and
- compact README continuity.

It does not change:

- public 40A plan construction;
- public 40A fresh reconstruction;
- generic 44A preparation;
- 47A transition persistence;
- 47B root persistence;
- 47C edge persistence;
- 47D adoption;
- 47F / public 40B persistence;
- any durable overlay format;
- second-basis overlay-v2;
- second-basis 37C/37D continuation formats;
- CLI dispatch;
- authority inspection;
- browser behavior; or
- semantic interpretation.

## Explicit stop boundary

50Q proves only third-basis fresh-process reconstructability of a bare selection
through 47E / public 40A.

Do not infer:

- 40B durable bare-selection serialization;
- successful 47F bare persistence;
- third-basis overlay-v2;
- third-basis persisted relaunch;
- 40C first continuation above bare third-basis ancestry;
- 40D cumulative continuation above bare third-basis ancestry;
- fourth-basis bare support;
- arbitrary Nth-basis version propagation;
- automatic locator discovery;
- current/latest/head authority;
- browser mutation;
- search or indexing;
- tags;
- export;
- citation authority; or
- semantic interpretation.

A fresh researcher-action review is required before widening 40B.

## Compact result

After 50Q:

```text
first-root bare cumulative ancestry
→ second-basis bare 37B-v2
→ cumulative second-epoch 37C-v1
→ third bare saved passage
→ unchanged 44A
→ unchanged 47A transition
→ unchanged 47B third root
→ unchanged 47C edge
→ explicit 47D adoption
→ widened 47E bare locator form
→ unchanged public 40A fresh reconstruction
→ all three bare passages retained with no manufactured notes
→ 47F / 40B overlay-v1 still fails closed
```

The durable version distinction remains confined to boundaries that actually own an
expanded member vocabulary.
