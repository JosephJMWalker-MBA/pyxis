# Pyxis Current Frontier — Milestones 46A through 46G

> **Continuity status — implementation through Milestone 46G / D234.**  
> **Updated:** 2026-08-29  
> This compact record does not replace milestone documents, implementation, tests, executed CI, or earlier frontier files.

## Read this after `docs/CURRENT_FRONTIER_44.md`

Milestones 44A–44H productized one explicit first changed evidence basis from an ordinary governed session through exact 35C typed handoff. Milestone 45 then made one-root launch provenance visibly inspectable without conflating immutable launch context with mutable current governed state.

The 46-series asked the next distinct product question:

> Can one exact one-root continuation explicitly cross a second changed evidence basis, establish and adopt the second root-backed governed session, prove and persist second-epoch restartability, and then explicitly transfer the exact fresh 37B proof into the already-established second-basis-epoch product?

Milestones 46A–46G now answer that question end to end.

## Completed second changed-basis product flow

```text
exact one-root 35D/35E continuation
→ inherited 44A changed-basis preparation
→ 46A persist second 33B transition
→ 46B persist second 34A revision root
→ 46C persist first 34B edge after second root
→ 46D explicitly adopt second changed-basis governed session
→ 46E freshly prove exact second-epoch reconstructability through public 37A
→ 46F persist verified second-epoch restart overlay through public 37B
→ 46G explicitly hand exact 37B fresh proof into established second-epoch product
```

Every arrow remains a separate authority action.

```text
preparation
!= transition
!= root
!= adoption
!= fresh reconstruction
!= durable restart configuration
!= in-process product handoff
```

---

## 46A / D228 — Explicit second changed-basis transition

46A reuses generic 44A preparation but requires exact one-root continuation authority before exposing another 33B crossing.

The retained `ChromiumResearchRootBackedSessionContinuationReentryResult` must own the exact supplied controller object. Structural equivalence is insufficient for this in-process authority action.

All durable transition locators remain caller supplied. No path is inferred from preparation receipts, continuation plans, launch provenance, checkpoint paths, filenames, directories, or hashes.

Successful 46A persistence leaves the mounted one-root continuation unchanged.

---

## 46B / D229 — Explicit second 34A revision root

46B consumes one exact successful 46A transition and one genuinely new human root rationale.

The prior edge, changed working set, changed working-set note, transition, and root destination are all explicit current durable locators.

The root is persisted and freshly reopened through public 34A machinery. It remains historical authority and does not itself create or adopt a second-epoch session.

---

## 46C / D230 — First ordinary edge after the second root

46C exposes the established 34B bridge from the exact 46B root back into ordinary edge lineage.

Only the current second-root source and a new no-overwrite edge destination are durable inputs, plus a new human post-root rationale.

The second root remains truthful historical authority even if the old one-root branch later rolls forward.

46C does not declare or adopt a second-epoch session.

---

## 46D / D231 — Explicit second changed-basis governed-session adoption

46D is the deliberate branch-changing action.

From the exact 46C root-plus-first-edge chain, it reuses established declaration/controller machinery to create and explicitly adopt the second changed-basis governed session.

The adoption is local to the shell and user authorized. It is not a global latest/current/head claim.

Successful adoption still creates no fresh-process restart configuration.

---

## 46E / D232 — Fresh second-epoch reconstruction proof

46E proves the exact historical 46D session can be freshly reconstructed through existing public 37A.

Every current durable locator required by 37A begins blank, including the prior 35D/35E continuation overlay. A prior launch path or earlier successful path is context, not perpetual current-location authority.

46E verifies both ancestry layers:

```text
retained first-root continuation ancestry
+ exact second-root session ancestry
```

It writes nothing and does not replace the mounted controller.

PR #185 merged 46E as `209326d7f02ac5e343810caad37a3be4a7a91597` after the first submitted head passed Repository Zero on Python 3.11–3.14. Python 3.11 executed 1192 tests.

---

## 46F / D233 — Persist verified second-epoch restart overlay

46F consumes the exact successful 46E proof and delegates persistence to public 37B.

The caller must explicitly re-supply:

1. the current prior 35D/35E continuation-overlay source;
2. a no-overwrite second-epoch 37B overlay destination.

Public 37B remains authoritative for mandatory two-layer fresh re-proof, overlay persistence, and strict round-trip decode.

The exact historical 46E target may be persisted even if the currently mounted product has subsequently rolled forward. Persistence does not retarget to the later controller and does not replace mounted governed state.

Raw 36D launch provenance remains pathless even when an explicitly supplied persisted prior overlay is used for proof.

PR #187 merged exact tested head `81d18a75f7ddee2eacc03f7b3cffb03f0f7b7cfb` as merge commit `494cf0b96004335439a087c0ad4b093ff6078a38`. Repository Zero run `33275465333` passed Python 3.11–3.14; Python 3.11 reported `1196 passed in 348.57s`.

---

## 46G / D234 — Explicit typed handoff into established second-epoch product

46G closes the remaining second changed-basis product seam.

Successful 46F persistence retains an exact public-37B mandatory fresh reconstruction:

```text
last_second_changed_basis_epoch_reentry_overlay
    .checkpoint
    .fresh_reentry
```

That exact `ChromiumResearchSecondBasisEpochReentryResult` is the 46G handoff subject.

It is not:

- the saved 37B overlay path;
- the earlier 46E fresh object merely because durable state is equivalent;
- a newly reloaded reconstruction;
- whichever controller happens to be mounted;
- a fabricated `ChromiumResearchSecondBasisEpochShellLineage`.

### Explicit choice remains separate from persistence

Successful 46F persistence alone leaves the prior product mounted.

Only the explicit 46G handoff action exits with the exact `checkpoint.fresh_reentry` object.

Normal close returns no handoff.

Deleting the just-written 37B overlay after successful 46F persistence does not invalidate the already-earned in-process typed handoff.

### Pathless initial second-epoch receiver

46G adds a concrete receiver that reuses the established second-epoch first-checkpoint product without inventing persisted launch provenance:

```text
second_basis_epoch_launch_lineage = None
second_basis_epoch_handoff_reentry = exact 46G handoff
second_basis_epoch_reentry = exact 46G handoff
```

The receiver therefore distinguishes:

```text
exact typed in-process authority
!= persistent path authority
```

After one explicit rollover, the existing public 37C checkpoint form appears with every durable locator blank.

### Read-only launch inspection

46G adds a concrete pathless initial-session inspection family. It records exact retained first-root, second-root, and launch-endpoint identities while setting launch location context to `None`.

Later rollover updates only current governed state. The exact immutable 46G launch-provenance object remains unchanged.

### Executed evidence

PR #189 merged exact tested head `b56bf6add407fadcc942b0b88bd9e95619e81a20` as merge commit `33f1d92b2052d3f1087c6561b30f178a6e9f415f`.

Repository Zero run `33278060940` passed the complete suite on Python 3.11, 3.12, 3.13, and 3.14 on the first submitted head. Python 3.11 reported:

```text
1203 passed in 278.26s
```

No corrective commit was required.

---

## Why there is no 46H

The fresh frontier review after 46G found no demonstrated missing step inside the second changed-basis lifecycle.

The established receiver already owns:

```text
46G exact initial second-epoch handoff
→ ordinary explicit rollover
→ public 37C first continuation checkpoint
→ explicit 38F typed handoff
→ repeatable public 37D cumulative continuation checkpointing
```

Persisted and in-process launch families already have explicit authority inspection. Adding another 46-series runtime step would therefore duplicate existing second-epoch continuation machinery or invent a new authority without a demonstrated product gap.

The dedicated 46G runner is an explicit product/API seam, like the earlier 44H runner. Its absence from an automatic CLI mode switch is not evidence that the handoff is incomplete; automatic promotion is intentionally rejected.

## 46-series strongest compact statement

> From one exact one-root continuation and exact candidate evidence, Pyxis can explicitly prepare and persist a second changed evidence basis, establish and adopt its second root-backed governed session, freshly prove and persist second-epoch restartability, and—only on a separate explicit user action—transfer the exact public-37B fresh proof into the established second-basis-epoch product without reloading or promoting the saved overlay path.

That product then already supports explicit 37C/38F/37D continuation.

## Authority boundaries still intentionally absent

Through 46G, Pyxis still does not infer or claim:

- a global current/latest/head research branch;
- chronology from paths, filenames, hashes, or filesystem timestamps;
- path equality as durable identity;
- persistent path authority from a previous operation;
- persistent path provenance for an in-process typed handoff;
- source authenticity, authorship, or trusted time from SHA-256 integrity;
- semantic improvement merely because a basis changed;
- evidentiary or citation support merely because notes or selections exist;
- autonomous research or browser authority;
- arbitrary-depth evidence-basis recursion;
- a generic `epoch[n]` transition/adoption/restart model;
- a fourth evidence-basis epoch;
- automatic restart or mode promotion from successful persistence.

## Next distinct frontier — 47A / D235

Issue #190 tracks the next separate product question:

> From one exact current `ChromiumResearchSecondBasisEpochContinuationReentryResult` and one successful generic 44A changed-basis preparation belonging to that exact controller, can Pyxis persist and freshly relink one third 33B cross-working-set transition without creating the third root or generalizing into arbitrary-depth epoch machinery?

This question is justified by two bodies of prior art:

1. **Internal:** the lower-level 40-series already proves that an explicitly located third basis-change region can be freshly reconstructed above persisted 37C/37D ancestry, while 46A provides the concrete product-side crossing precedent.
2. **External:** W3C PROV, DVC, DataLad, and Renku provide mature provenance/versioning/reproducibility concepts, but the review did not demonstrate an end-to-end substitute for Pyxis's exact human-authorized basis-crossing authority contract.

Conclusion: **no end-to-end substitute demonstrated in this review**.

47A is explicitly not implemented merely because this continuity record identifies the approved next question.

## Continuity rule

For future development sessions:

```text
implementation + executed tests + milestone record
> compact frontier continuity summary
> README/current-status wording
```

If this compact document conflicts with implementation, tests, executed CI, or milestone-specific records, inspect the stronger evidence before changing behavior.
